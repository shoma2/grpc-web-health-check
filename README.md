# grpc-web-health-check

health check for grpc and grpc-web

## usage

```sh
npx gwhc --address localhost:50051 --insecure --debug
```

## run grpc server implemented health.proto and grpc-web proxy

```sh
npm run serve
```